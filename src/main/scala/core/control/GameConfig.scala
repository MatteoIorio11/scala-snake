package core.control

case class GameConfig(
                       boardWidth: Int,
                       boardHeight: Int,
                       cellSize: Int,
                       updateIntervalMs: Int)

